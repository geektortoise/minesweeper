import { BiDimensionalBoard } from './bi-dimensional-board';

describe('BiDimensionalBoard', () => {
  it('should create an instance', () => {
    expect(new BiDimensionalBoard(10, 10)).toBeTruthy();
  });
});
